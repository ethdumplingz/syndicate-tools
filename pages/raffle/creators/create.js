import {Grid, Typography} from "@mui/material";

const createView = (props) => {
	return(
		<Grid
			container
		>
			<Grid item>
				<Typography>
				
				</Typography>
			</Grid>
			<Grid item>
			
			</Grid>
		</Grid>
	)
}

export default createView;